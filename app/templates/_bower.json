{
  "name": "<%= modulenameDashed %>",
  "main": [
    <% if (angular) { %>"/dist/<%= modulenameDashed %>.min.js"<% } %><% if (angular && stylus) { %>,
    <% } %><% if (stylus) { %>"/dist/<%= modulenameDashed %>.min.css"<% } %>
  ],
  "version": "0.1.0",
  "description": "TODO",
  "authors": [
    "TODO"
  ],
  "ignore": [
    "**/.*",
    "package.json",
    "klei.json",
    "gulpfile.js",
    "node_modules",
    "bower_components",
    "src"
  ],
  "dependencies": {
    <% if (angular) { %>"angular": "~1.2.14",
    "angular-route": "~1.2.14"<% if (express) { %>,
    "restangular": "~1.3.1"<% } %><% } %>
  }<% if (angular) { %>,
  "devDependencies": {
    "angular-mocks": "~1.2.14"
  }<% } %>
}
